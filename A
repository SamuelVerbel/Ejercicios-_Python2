horas = int(input("Ingrese las horas trabajadas: "))
valor_hora = float(input("Ingrese el valor por hora: "))

if horas > 40:
    extra = horas - 40
    sueldo = (40 * valor_hora) + (extra * valor_hora * 1.5)
else:
    sueldo = horas * valor_hora

print("El sueldo del trabajador es:", sueldo)
